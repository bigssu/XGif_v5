# ═══════════════════════════════════════════════════════════════
# XGif 의존성 패키지
# 버전 범위 명시로 호환성 보장
# ═══════════════════════════════════════════════════════════════

# UI 프레임워크 (wxPython)
wxPython>=4.2.0,<5.0.0  # 네이티브 Windows GUI 프레임워크

# 화면 캡처
dxcam>=0.0.5,<1.0.0  # Win11 고성능 화면 캡처 (DXGI Desktop Duplication)

# 이미지 처리
numpy>=2.0.0,<3.0.0  # SIMD 최적화 개선
Pillow>=10.0.0,<11.0.0  # 이미지 I/O
imageio>=2.33.0,<3.0.0  # 현대적인 이미지 I/O
# imageio-ffmpeg 제거됨 — 시스템 FFmpeg 사용 (IMAGEIO_FFMPEG_EXE 환경변수)

# 비디오 처리 (FFmpeg는 subprocess로 직접 호출)

# GPU 모니터링
nvidia-ml-py>=12.0.0,<13.0.0  # pynvml - GPU 상세 정보

# 시스템 모니터링
psutil>=5.9.0,<6.0.0  # 시스템 리소스 모니터링

# 오디오 처리
sounddevice>=0.4.6,<1.0.0  # 오디오 녹음
soundfile>=0.12.0,<1.0.0  # 오디오 파일 I/O
# pydub 제거됨 — FFmpeg subprocess로 오디오 병합 처리


# 키보드 입력 감지
pynput>=1.7.6,<2.0.0

# ═══════════════════════════════════════════════════════════════

# 오디오 병합 (scipy 없으면 FFmpeg 사용)
# scipy>=1.10.0,<2.0.0

# GPU 가속 (NVIDIA GPU가 있는 경우에만 설치)
# CUDA 12.x: pip install "cupy-cuda12x>=12.0.0,<13.0.0"
# CUDA 11.x: pip install "cupy-cuda11x>=11.0.0,<12.0.0"
# 자동 선택: pip install "cupy>=12.0.0,<13.0.0"

# ═══════════════════════════════════════════════════════════════
# 개발 및 테스트 도구 (선택적)
# ═══════════════════════════════════════════════════════════════
# pytest>=7.4.0,<8.0.0  # 단위 테스트
# pytest-cov>=4.1.0,<5.0.0  # 코드 커버리지
# mypy>=1.5.0,<2.0.0  # 타입 체킹
# black>=23.0.0,<24.0.0  # 코드 포맷팅

# 앱 수정시 버전 up.
