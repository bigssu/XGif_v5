# Tabler Icons subset

This directory contains only the SVG icons used by the XGif editor toolbars.

Source: https://github.com/tabler/tabler-icons
License: MIT, see `LICENSE`.

The subset is intentionally vendored instead of adding an icon package dependency, keeping the packaged app smaller and the toolbar icon style consistent.
